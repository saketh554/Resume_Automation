# ResumeFit formatting rules

- Use Python type hints on public functions.
- Keep functions focused and avoid deep nesting.
- Use clear names over comments where possible.
- Keep user-facing messages concise and actionable.
- Avoid introducing non-ASCII characters unless required.
