# ResumeFit coding standards

- Keep implementations small, typed, and readable.
- Use Pydantic schemas for every structured LLM output.
- Never fabricate resume history; keep suggestions grounded.
- Prefer explicit failure handling over silent fallback.
- Preserve .docx formatting and length constraints when editing resumes.
